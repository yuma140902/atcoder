#[allow(unused_imports)]
use std::collections::*;

fn count(bits: &Vec<bool>, q: &Vec<i64>, a: &Vec<i64>, b: &Vec<i64>) -> i64 {
    for i in 0..bits.len() {
        if bits[i] && a[i] != 0 {}
    }
}

#[argio::argio]
fn main(n: usize, q: [i64; n], a: [i64; n], b: [i64; n]) -> i64 {
    for bits in 0..2_i64.pow(n as u32) {}
    todo!();
}
